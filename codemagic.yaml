workflows:
  ios-release:
    name: iOS Build
    max_build_duration: 60

    environment:
      flutter: stable

    scripts:
      - flutter pub get
      - flutter build ipa --no-codesign

    artifacts:
      - build/ios/ipa/*.ipa